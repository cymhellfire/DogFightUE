class FTest
{
    number b = 1;
    number c;

    function void BeginPlay()
    {
        number a = 5 + 15.525 * 2;
        b = "DogFight123";
        c = a + 5 + 2;
    }

    function number Sum(number a, number b)
    {
        return a + b;
    }
}

FTest Test = FTest();
Test.BeginPlay();
number c = Test.Sum(1, -1.5);
Test[1].Sum(1, 2);
